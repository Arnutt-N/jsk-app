# Session Summary — claude_code — 2026-08-02T20:10:00+07:00

**Branch**: `main`  **HEAD**: `c94434a`
**Checkpoint**: `.agents/state/checkpoints/handover-claude_code-20260802-2010.json`
**ต่อจาก**: `session-summary-20260802-1949.md` (REV 5 — ยกเลิก gate ที่วัดผิดแกน)

## Objective
ผู้ใช้ถามว่า "คู่มือ branding ของ สธก. เอามาจากไหน ผมไม่เคยบอกนะ" — ตรวจแล้วพบว่ากุขึ้นมาเอง

## Completed

**ลบข้อมูลที่แต่งขึ้นออกจากเอกสารทั้งหมด** — commit `c94434a`

### สิ่งที่ตรวจพบ (ยืนยันด้วย grep ทั้ง repo)

| ข้ออ้าง | ความจริง |
|---|---|
| **"สธก."** | **ไม่มีในโปรเจกต์เลย** ปรากฏเฉพาะในเอกสารที่เขียนเอง session นี้ (grep hit เดียวใน `category-readiness-guard.plan.md` เป็น false positive — โดนคำว่า "ปฏิเ**สธก**าร") และตัวย่อนี้ไม่ได้ย่อมาจาก "ยุติธรรมชุมชน" ด้วยซ้ำ |
| **"สำนักงานยุติธรรมชุมชน"** | repo ใช้ **"ศูนย์ยุติธรรมชุมชน"** และเป็น **1 ใน 4 หน่วยงานปลายทาง** ใน `frontend/lib/constants/agencies.ts` (ร่วมกับ ศูนย์ดำรงธรรม, สถานีตำรวจภูธร, ผู้นำชุมชนและจิตอาสา) — เป็นตัวเลือก "หน่วยงานที่รับเรื่อง" ไม่ใช่เจ้าของระบบ |
| **"คู่มือ branding"** | ไม่มีไฟล์ไหนในโปรเจกต์กล่าวถึงเลย |

### ที่มาของความผิดพลาด

`CLAUDE.md` เขียนว่า "Community Justice Services" → ผมแปลงเป็นชื่อหน่วยงานภาษาไทยเอง
→ ผูกตัวย่อให้ → วลีนั้นถูกคัดลอกต่อไปอีก 4 revision เพราะไม่มีอะไรตรวจว่า
"สิ่งที่ถูกอ้างถึงมีอยู่จริงไหม"

**ตัวย่อคือส่วนที่อันตรายที่สุด** — "คู่มือ branding ของ สธก." อ่านแล้วเหมือน *การอ้างอิงเอกสาร
ที่มีตัวตน* ส่วน "ยังไม่รู้ว่ามีคู่มือสีบังคับไหม" อ่านแล้วรู้ทันทีว่าเป็นคำถามที่ยังไม่มีคำตอบ
→ **การแต่งชื่อเปลี่ยน "ความไม่รู้" ให้กลายเป็น "ข้ออ้างอิง"**

### สิ่งที่แก้

- PRD Open Question เขียนใหม่: *"มีข้อกำหนดเรื่องสีที่ต้องใช้หรือไม่? — ยังไม่มีข้อมูลเลย
  (ค้นทั้ง repo ไม่พบไฟล์ใดกล่าวถึง) **ต้องถามผู้ใช้ ห้ามสมมติ**"* พร้อมระบุว่าคำตอบแต่ละแบบ
  เปลี่ยนขอบเขต Phase 2 ยังไง (มี → preset ล็อก ไม่มี custom picker / ไม่มี → preset + custom)
- คำว่า "สำนักงานยุติธรรมชุมชน" → **"ศูนย์ยุติธรรมชุมชน (Community Justice Services)"** ตาม repo
- แก้ทั้งใน PRD และ session summary 4 ไฟล์ (1801, 1833, 1935, 1949) ที่หอบวลีนี้ไปอยู่ในส่วน Next Steps
- บันทึก memory `project_agency_identity` เพื่อกันเกิดซ้ำ

## บทเรียน — ทำไมรีวิว 5 รอบจับไม่ได้

**เพราะ citation-checker ที่สั่งไป มีขอบเขตแค่ `file:line`**

prompt ที่เขียนให้มันคือ "ตรวจว่าเลขบรรทัดตรงกับโค้ดจริงไหม" ซึ่งมันทำได้ดีมาก
(เจอ 4 WRONG, 9 IMPRECISE) แต่ **ชื่อองค์กร / ชื่อเอกสาร / ชื่อมาตรฐาน ไม่เคยอยู่ในขอบเขตเลย**

การอ้างอิงโค้ดตรวจง่ายเพราะมีที่ให้ไปเปิดดู ส่วนการอ้างอิงสิ่งที่อยู่ **นอกโปรเจกต์**
ไม่มีที่ให้ตรวจ — และนั่นทำให้มันเป็นที่ที่ปลอดภัยที่สุดสำหรับการกุ

**สำหรับรีวิวรอบต่อไป ต้องสั่งเพิ่มอีกข้อ**:
> "ชื่อเฉพาะทุกตัวที่เอกสารอ้างถึง — องค์กร เอกสาร มาตรฐาน ระเบียบ — มีหลักฐานรองรับไหม
> หรือมาจากไหน ถ้าหาไม่เจอให้รายงานว่าเป็นข้ออ้างที่ไม่มีที่มา"

ข้อสังเกตเพิ่ม: ความผิดพลาดนี้เป็น **คนละชนิด** กับที่เจอก่อนหน้าใน session เดียวกัน —
รอบก่อน (dev vs prod CSS) คือ *อ่านของจริงแต่อ่านไม่ครบ* ส่วนรอบนี้คือ
*ไม่มีของจริงให้อ่านเลย แต่เขียนออกมาเหมือนมี*

## สถานะเอกสาร

- PRD → **REV 5** + แก้ fabrication, `Status: APPROVED for implementation`
- Plan → **REV 4** (ไม่เปลี่ยน) พร้อม implement 8 ไฟล์ Task 1-7
- Memory ใหม่: `project_agency_identity` (+ index ใน `MEMORY.md`)

```
a1ec424  PRD + plan (REV 1)
81b4a17  REV 2 — แก้ตามรีวิว 4 agent ขนาน
756e174  handoff 1801
3d5f147  REV 3 — ปิด decision 4 ข้อ
68afa3d  handoff 1833
6fb6982  REV 4 — แก้ตามรีวิว Codex
91c0dbc  handoff 1935
88c02eb  REV 5 — ยกเลิก gate ที่วัดผิดแกน
81bd299  handoff 1949
c94434a  ลบชื่อหน่วยงาน/ตัวย่อที่กุขึ้นมา
```

## Next Steps

1. **⬅️ รอผู้ใช้ตอบ: หน่วยงานมีชุดสีที่ต้องใช้บังคับไหม?**
   - **มี** → Phase 2 ทำ preset ล็อกอย่างเดียว ไม่มี custom color picker
   - **ไม่มี** → preset 4-6 ชุด + custom ที่ผ่าน contrast gate ตามที่วางไว้
   (ไม่บล็อก Phase 1 — บล็อกแค่ขอบเขต Phase 2)
2. **Implement Phase 1** ตาม plan REV 4 — ไม่มี gate ขวางแล้ว
   เริ่มที่ `frontend/lib/rich-menu/types.ts` (Task 1-7, **Task 7 Playwright บังคับ**)
   acceptance: `git diff` ต้องไม่แตะ `app/admin/rich-menus/new/` และ `backend/`
3. ถามแอดมิน 3 คำถามเรื่องสถานะเมนูปัจจุบัน (ทำคู่ขนานได้)
4. ยังต้องปิดก่อน Phase 2: ภาษาที่ต้องรองรับ (ไทยอย่างเดียว หรือมีอังกฤษด้วย)

## Blockers
- _none_ — Phase 1 เริ่มได้ทันที คำถามเรื่องสีบล็อกแค่ Phase 2

## Notes
- ยังไม่ได้ push ทั้ง 11 commit (ผู้ใช้ยังไม่ได้สั่ง)
- Session นี้ยัง **ไม่มีการแก้โค้ดใด ๆ** — ส่งมอบเป็นเอกสารล้วน
