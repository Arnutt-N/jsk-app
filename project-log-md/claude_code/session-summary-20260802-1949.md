# Session Summary — claude_code — 2026-08-02T19:49:00+07:00

**Branch**: `main`  **HEAD**: `88c02eb`
**Checkpoint**: `.agents/state/checkpoints/handover-claude_code-20260802-1949.json`
**ต่อจาก**: `session-summary-20260802-1935.md` (REV 4 — แก้ตามรีวิว Codex)

## Objective
เจ้าของงานคัดค้าน gate ที่ REV 4 เพิ่มเข้ามา — ตรวจแล้วคัดค้านถูก จึงยกเลิกและเปลี่ยนวิธีหาหลักฐาน

## Completed

**ยกเลิก gate "ถ้าสร้าง < 1 เมนู/เดือน ให้หยุดโครงการ"** — commit `88c02eb`

### ทำไม gate นั้นผิด

**Rich menu เป็นโครงสร้างพื้นฐานที่อยู่ถาวร ไม่ใช่งานที่ทำซ้ำ** — สร้างครั้งเดียวแล้วผู้ติดตาม OA
ทุกคนเห็นมันทุกวันไปอีกเป็นปี **ความถี่ของงานแอดมินจึงไม่สะท้อนคุณค่าของผลลัพธ์**
(ตรรกะเดียวกันจะสรุปว่าเครื่องมือ deploy ไม่ควรมี เพราะ deploy ไม่บ่อย)

และที่แย่กว่า: **ตัวเลขต่ำอาจเป็นหลักฐาน *สนับสนุน* ฟีเจอร์นี้** ถ้าแอดมินไม่ค่อยแก้เมนู
เพราะแก้ทีต้องไปตามหาคนทำกราฟิก ตัวเลขต่ำคือ *อาการของโรค* ไม่ใช่ข้อสรุปว่าไม่มีความต้องการ
— **การนับอย่างเดียวแยกสองกรณีนี้ไม่ออก** (เคยบันทึกข้อสังเกตนี้ไว้ใน REV 4 แล้วแต่ยังคง gate ไว้อยู่ดี)

### สิ่งที่ใช้แทน

เปลี่ยนจาก "ถามอัตราการเปลี่ยนแปลง" เป็น **"ถามสถานะปัจจุบัน"** ตอบด้วยการถามแอดมิน
2-3 คนที่ถือ `KEY_MANAGE_RICH_MENUS`:

1. **เมนูที่ live อยู่ตอนนี้ ตรงกับบริการที่หน่วยงานให้จริงหรือเปล่า?**
   (เมนูตั้งไว้นานแล้วบริการเปลี่ยนไป = หลักฐานของ friction ไม่ใช่ของความพอใจ)
2. **มีอะไรที่อยากแก้ในเมนูแต่ยังไม่ได้แก้ไหม เพราะอะไร?** ← ความตั้งใจที่ถูกบล็อก = หลักฐานตรงที่สุด
3. **ครั้งล่าสุดที่แก้ ใช้เวลาเท่าไร ต้องรอใครไหม?**

**เกณฑ์เดินต่อ**: มีอย่างน้อย 1 คนตอบข้อ 2 ว่า "มี" → ปัญหามีจริง
ส่วน `SELECT count(*), min(created_at), max(created_at) FROM rich_menus;` **ลดชั้นเป็นข้อมูลประกอบ**
ของคำถามข้อ 1 (ช่วยดูว่าเมนูถูกแตะครั้งสุดท้ายเมื่อไร) ไม่ใช่ตัวตัดสิน

### ผลต่อสถานะ

| | REV 4 | REV 5 |
|---|---|---|
| Gate | สร้าง < 1 เมนู/เดือน → หยุด | ❌ ยกเลิก |
| หลักฐาน | นับด้วย SQL | ถามแอดมิน 3 คำถาม (SQL = ข้อมูลประกอบ) |
| Phase 1 | technical spike ที่ยังไม่ merge | **APPROVED — เริ่มและ merge ได้** |

plan ไม่ได้อ้าง gate นี้เลย (`grep` = 0) จึงไม่ต้องแก้ ไฟล์เดียวที่แตะคือ PRD

## บทเรียน — ทำไม gate ผิดนี้อยู่รอดผ่าน 5 รอบรีวิว

เพราะ **ทุกรอบตรวจคนละอย่าง แต่ไม่มีรอบไหนตรวจสมมติฐานร่วม**:

- agent รีวิว PRD **เป็นคนเสนอ** gate นี้ (ในฐานะข้อเสนอเรื่อง ROI)
- ผมรับมาโดยไม่ได้ตรวจว่า "ความถี่ของงาน" กับ "คุณค่าของฟีเจอร์" เป็นคนละแกน
- Codex ชี้ว่า *"PRD ยอมให้ implement ก่อนเช็ค gate ซึ่งไม่สอดคล้องกันเอง"* — **ถูกต้องเชิงตรรกะภายใน**
- ผมจึงแก้ให้สอดคล้องโดยทำ gate ให้ **แข็งขึ้น** (ลดสถานะ Phase 1 เป็น spike)

รีวิวตรวจ **ความสอดคล้องภายใน** ได้ดีมาก แต่ตรวจ **สมมติฐานที่ทุกฝ่ายใช้ร่วมกัน** ไม่ได้
เพราะมันคือพื้นที่ที่ทุกคนยืนอยู่ ต้องมีคนที่รู้จักงานจริงมาชี้

**และมันเปลี่ยนทิศทางงานจริง**: ถ้าเชื่อ gate เดิม ขั้นต่อไปคือไปรัน query แล้วอาจปิดโครงการทิ้ง
ตอนนี้ขั้นต่อไปคือเริ่มเขียนโค้ดได้เลย

## สถานะเอกสาร

- PRD → **REV 5**, `Status: APPROVED for implementation` ไม่มี gate ขวาง
- Plan → **REV 4** (ไม่เปลี่ยน) พร้อม implement 8 ไฟล์ Task 1-7
- Review artifact → `.claude/PRPs/reviews/rich-menu-image-generator-codex-review.md`

```
a1ec424  PRD + plan (REV 1)
81b4a17  REV 2 — แก้ตามรีวิว 4 agent ขนาน
756e174  handoff 1801
3d5f147  REV 3 — ปิด decision 4 ข้อ
68afa3d  handoff 1833
6fb6982  REV 4 — แก้ตามรีวิว Codex
91c0dbc  handoff 1935
88c02eb  REV 5 — ยกเลิก gate ที่วัดผิดแกน
```

## Next Steps

1. **Implement Phase 1** — ไม่มีอะไรขวางแล้ว เริ่มที่ `frontend/lib/rich-menu/types.ts`
   ตาม plan REV 4 (Task 1-7, **Task 7 Playwright บังคับ**)
   acceptance: `git diff` ต้องไม่แตะ `app/admin/rich-menus/new/` และ `backend/`
2. **ถามแอดมิน 3 คำถาม** — ทำคู่ขนานได้ ไม่บล็อกการเขียนโค้ด คำตอบใช้กำหนดขอบเขต Phase 2
3. ก่อนเข้า Phase 2 ยังต้องปิด: คู่มือ branding ของ สธก. + ภาษาที่ต้องรองรับ

## Blockers
- _none_ — Phase 1 เริ่มได้ทันที

## Notes
- ยังไม่ได้ push ทั้ง 9 commit (ผู้ใช้ยังไม่ได้สั่ง)
- Session นี้ยัง **ไม่มีการแก้โค้ดใด ๆ** — ส่งมอบเป็นเอกสารล้วน
