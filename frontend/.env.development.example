NEXT_PUBLIC_API_URL=https://api.example.com/api/v1
NEXT_PUBLIC_DEV_MODE=false

# Cookie auth is unconditional (mode-flag cleanup removed COOKIE_AUTH_MODE).
# Backend rollback: redeploy a pre-cleanup backend image.
