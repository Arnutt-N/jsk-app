NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
NEXT_PUBLIC_DEV_MODE=false

# Cookie auth is now the default (PR 2C). No frontend flag needed.
# Backend rollback: set COOKIE_AUTH_MODE=dual or bearer in backend .env
